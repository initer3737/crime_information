# TODO

- [x] Update `src/router.ts`: route `/:id/data` -> `Data.vue`

- [ ] Refactor `src/Data.vue`: ambil `id` dari `useRoute`, validasi, perbaiki query/error handling, rapikan template (1 loop, render Youtube/Facebook sesuai `type`)

- [ ] Run `npm run dev` (atau `npm run build`) untuk memastikan tidak ada error
